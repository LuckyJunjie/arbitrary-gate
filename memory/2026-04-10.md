# 2026-04-10 开发进度

## 今日完成

### C-09 残片拼接擦墨动画 ✅
**文件**: `src/frontend/components/ScratchCard.vue`

实现内容：
- **clip-path 渐进露出**：卡片初始时通过 `clip-path: inset(80% 0 0 0)` 隐藏底部 80%，仅露出顶部 1/5。随着擦除进度增加，clip-path 逐渐打开（`Math.round((100 - eraseProgress) * 0.80)`）
- **Canvas 墨迹渐淡**：`canvas` 添加动态 `:style="{ opacity: 1 - eraseProgress / 100 }"`，随擦除进度线性变淡，与 clip-path 配合实现自然显现
- **墨迹滴落动画**（已存在）：`inkDrop` CSS 动画，translateY + opacity 效果
- **提示文字**：`font-family: '方正清刻本悦宋', 'FZQingKeBenYueSong', 'STKaiti', 'KaiTi', serif`

### E-07 关键词落位可视化 ✅
**文件**: `src/frontend/views/StoryView.vue`

实现内容：
- 新增 `keywordColumnRoles` 和 `keywordPositionsByRole` computed 属性
- 入局弹窗底部新增三列布局（核心意象 / 转折道具 / 人物关联）
- 每个关键词卡片带泥金色边框，标注角色名，下方有渐变分隔线（`connector-line`）
- 空列显示"未分配"提示
- `KeywordPosition` 类型已在 `api.ts` 中定义（`role: '核心意象' | '转折道具' | '人物关联'`）

### UI-08 进度墨线完善 ✅
**文件**: `src/frontend/views/StoryView.vue`

增强进度墨线视觉效果：
- 墨褐色渐变背景（`#1a1208 → #6b4020 → #1a1208`）
- 流动动画 `inkFlow`（4s 线性循环，背景位置移动）
- 墨色投影 `box-shadow: 0 0 6px rgba(42, 31, 20, 0.5)`

### S-16 断线重连完整方案 ✅
后端已完整实现：
- `StoryOrchestrationService.getChapterProgress()`: 返回 `{ chapterNo, generatedLength }`
- `saveChapterProgress()`: 每次 SSE/WSS 推送时 append 到 `chapter.sceneText`
- `StoryController.getChapterProgress()`: REST API 端点

前端 storyStore 已实现：
- 指数退避重连（1s/2s/4s/8s，最多 5 次）
- `fetchChapterProgress` 调用获取服务端已生成长度
- `saveDraft/getDraft/clearDraft` localStorage 管理

注：`onReconnect` 回调目前只恢复本地草稿并重新 fetch 章节，不请求服务端 partial text（因 getChapterProgress 只返回长度）。可作为后续优化点。

### P-03 三器物成就【物是人非】✅
已在 `achievementStore.ts` 完整实现：
- `combo_three_objects`（物是人非）：3 张 category=1 卡牌
- `combo_three_emotions`（百感交集）：3 张 category=4 卡牌
- `combo_departing_person`（离人）：旧船票 + 摆渡人/乌江渡
- 相关测试用例在 `achievementStore.test.ts` 中（行 287-376）

## 测试状态
- 单元测试：243/243 ✅
- E2E：部分 flaky 测试失败（ai-painter 外部 API，书架模块需要后端），与本次改动无关

### M-03 题记生成 ✅
**文件**: 多文件协同（见下）

实现内容：
- **后端 Entity**: `StoryManuscript.inscription` 字段（VARCHAR，散文诗风格）
- **后端 AI**: `StorytellerAgent.generateInscription()` — 说书人 prompt: `"为本故事写一句题记，散文诗风格，不超过30字"`
- **后端 Service**: `StoryOrchestrationService.finishStory()` — 生成正文后追加题记调用，存入 `manuscript.inscription`
- **后端 VO**: `FinishStoryVO.inscription` 字段
- **前端展示**: `ManuscriptView.vue` — 正文前居中竖排展示题记，赭石色 `#8b5e3c`，`writing-mode: vertical-rl`
- **前端 Mock**: `storyTeller.generateInscription()` + `mockFinishStory` 返回值含 `inscription`

注：`inscription` 字段已在本次任务前完整实现，subagent 超时前确认全部到位。

### C-08 墨迹占卜（今日运势）✅
**文件**: `src/frontend/views/PoolView.vue`

实现内容：
- 后端 `CardController.getFortune()` + `CardService.getFortune()` 已完整实现（无需新建文件）
- 前端 `PoolView.vue` 在 `onMounted` 中增加 localStorage 缓存逻辑：key = `fortune:{date}`（如 `fortune:Fri Apr 10 2026`）
- 同一天再次访问时，直接从 localStorage 读取 `fortune` 和 `hint`，不重复请求 API
- 首次访问当天才真正调用 `/api/card/fortune`，并缓存结果

### AI-06 AI腔词黑名单过滤 ✅
**文件**: `src/backend/com/timespace/module/ai/util/AiPhraseFilter.java`, `JudgeAgent.java`, `BaiguanAgent.java`

实现内容：
- 黑名单新增4个短语：`"缓缓道来"`, `"不禁微微一怔"`, `"眼中闪过"`, `"轻轻叹了口气"`
- 黑名单静态初始化块（`BLACKLIST`）和 `deleteWords` Set 均已更新
- `JudgeAgent.evaluate()` 输出中 `judgment`（判词）已添加 `AiPhraseFilter.filter()` 后处理
- `BaiguanAgent.generateAnnotation()` 输出（稗官批注）已添加 `AiPhraseFilter.filter()` 后处理
- `StorytellerAgent` 章节场景和手稿均已使用 filter（本轮之前已实现）
- `BaiguanAgent` 后日谈和总评均已使用 filter（本轮之前已实现）

### E-08 风格选项 UI ✅
**文件**: `src/frontend/views/EntryQuestionsView.vue`, `StoryController.java`, `StoryOrchestrationService.java`, `StorytellerAgent.java`

实现内容：
- 前端 `EntryQuestionsView.vue` 已有4选项卡"白描/江湖/笔记/话本"，选中后泥金色边框高亮
- `selectedStyle` 默认值 1（白描），提交时传入 `submitEntryAnswers({ ..., style: selectedStyle })`
- 后端 `AnswersRequest.style` + `StartStoryRequest.style` 字段已存在
- `StoryOrchestrationService.submitEntryAnswers()` 将 style 写入 `story.style`
- `StorytellerAgent.buildChapterSystemPrompt()` 调用 `getStyleGuidance(style)` 切换4种叙述口吻 prompt
- E-08 本次之前已完整实现，本次确认为已就绪状态

## 后续工作
1. E-07: 确保后端 `submitEntryAnswers` / `startStory` 返回的 story 对象包含 `keywordPositions`（已确认在 `StartStoryVO` 中设置）
2. S-16: 可考虑添加服务端 partial text 返回（需要新 API 端点 `GET /api/story/{id}/chapter/{no}/partial`）
3. E2E flaky tests：需要网络/后端环境，可 skip

---

## 晚上补充（C-08 + AI-06 重构）

### C-08 运势文案 JSON 外置 ✅
- 新建 `src/backend/resources/fortune.json`，包含 34 条运势文案（含 fortune + hint 字段）
- 修改 `CardService.java`：`fortuneLibrary` 从静态硬编码改为 `@PostConstruct` 加载 classpath JSON
- 加载失败时 fallback 到硬编码 List（`getDefaultFortuneLibrary()`）
- hash 算法保持不变（同一用户同一天结果一致）

### AI-06 AiPhraseFilter Spring Bean 化 ✅
- `AiPhraseFilter.java` 添加 `@Component` + `@PostConstruct` 初始化
- `BLACKLIST` 和 `PATTERNS` 改为实例变量（非 static）
- `deleteWords` 改为构造函数初始化
- 3个 Agent（`JudgeAgent`、`BaiguanAgent`、`StorytellerAgent`）均注入 `AiPhraseFilter` 并使用实例方法
- 单元测试全部通过（261/261）

---

## 晚间补充（opus P1/P2 任务）

### AI-08 AI 内容安全检测 ✅
**文件**: `src/backend/com/timespace/module/story/service/StoryOrchestrationService.java`

实现内容：
- `finishStory()` 中，ZhangyanAgent 过滤 AI 腔后，对手稿正文进行二次内容安全检测
- 检测不通过时：调用 `generateManuscriptWithTitles()` 重新生成（带关键词强调 prompt），最多重试
- 二次检测仍不通过：使用兜底文案 `（此篇故事，因时局所限，暂时无法完整呈现）`
- 已在 `AIGatewayService.generateManuscriptWithTitles()` 中有一次安全检测，本次为二次保险

### AI-09 关键词融入率检测 ✅
**文件**:
- 新建 `src/backend/com/timespace/module/ai/util/KeywordInsertionChecker.java`
- 修改 `src/backend/com/timespace/module/story/service/StoryOrchestrationService.java`

实现内容：
- 新建 `KeywordInsertionChecker`：检测手稿中关键词融入数是否 ≥3
- 融入 ≥3 → `isSufficient=true`，无警告
- 融入 <3 → `isSufficient=false`，返回警告信息（如 `"【关键词融入提醒】本篇手稿仅融入 2/3 个关键词..."`）
- `finishStory()` 调用 `KeywordInsertionChecker.check()`，结果存入 `FinishStoryVO.keywordWarning` 字段
- 关键词融合不足时警告，但不阻止手稿展示（符合需求）

### C-07 手牌上限检查 ✅
**文件**: `src/backend/com/timespace/module/card/service/CardService.java`, `BusinessException.java`

实现内容：
- `drawKeywordCard()`: 关键词卡上限 9 张，超限抛出 `KEYWORD_CARD_LIMIT_REACHED`
- `drawEventCard()`: 事件卡上限 3 张，超限抛出 `EVENT_CARD_LIMIT_REACHED`
- `BusinessException` 新增 `KEYWORD_CARD_LIMIT_REACHED` / `EVENT_CARD_LIMIT_REACHED` 两个独立错误码
- 用户不能继续抽卡直到使用或回炉卡牌

### P-01 组合判词生成 ✅
**文件**:
- 修改 `src/backend/com/timespace/module/story/controller/StoryController.java`
- `src/backend/com/timespace/module/card/service/CardService.generatePreviewJudgment()` 已存在（本轮前）

实现内容：
- 新增 `POST /api/story/preview-judgment` API 端点
- 请求：`{ keywordIds: Long[], eventId: Long }`
- 响应：`{ judgment: String }`（如 `"风云将起，命悬一线。"`）
- 底层调用 `CardService.generatePreviewJudgment()`（AI 生成古文判词）

### B-05 书架视觉 — 老式书架质感 UI ✅
**文件**: `src/frontend/views/BookshelfView.vue`

实现内容：
- grid 视图容器添加 `data-testid="bookshelf-wooden-frame"`
- 木质背景纹理（多层 linear-gradient 叠加）已在本轮前完整实现
- 书架底板（shelf-plank）、书脊（book-spine）、题签效果均已存在

### 单元测试
- 新增 `KeywordInsertionCheckerTest.java`（15 个测试用例）
- 全部测试：261/261 ✅

---

## 晚间补充（S-11 手势轻重缓急传后端）

### S-11 手势轻重缓急传后端 ✅
**文件**: 多文件协同

实现内容：
- **后端 `ChooseRequest`**: 新增 `gestureIntensity` 字段（gentle/urgent/forceful）
- **后端 `StoryController`**: `submitChoice` 和 `submitChoiceAndStream` 将 `request.getGestureIntensity()` 传入 Service 层
- **后端 `StoryOrchestrationService`**: `submitChoice()` 和 `submitChoiceAndStream()` 新增 `String gestureIntensity` 参数，透传给 `AIGatewayService.generateNextChapter()`
- **后端 `AIGatewayService`**: `generateNextChapter()` 新增 `gestureIntensity` 参数，透传给 `StorytellerAgent.generateChapter()`
- **后端 `StorytellerAgent`**: 
  - `generateChapter()` 新增 `gestureIntensity` 参数
  - `buildChapterSystemPrompt()` 新增 `gestureIntensity` 参数
  - 新增 `buildGestureGuidance(gestureIntensity)` 方法，根据手势类型注入叙事口吻提示：
    - `gentle` → 轻柔舒缓，如微风拂面，叙事悠缓从容
    - `urgent` → 紧凑急促，情节推进迅速，时间紧迫感
    - `forceful` → 浓烈有力，戏剧张力强，冲击与震撼感
- **前端 `StoryView.vue`**: `selectOption()` 正式使用 `_intensity` 参数（原为废弃名），传递给 `storyStore.submitChoice()`
- **前端 `storyStore.ts`**: `submitChoice()` 新增 `intensity?` 参数，透传给 `submitChoiceWithMock()`
- **前端 `api.ts`**: `mockSubmitChoice()` 新增 `_gestureIntensity` 参数（mock 模式忽略）

**叙事口吻影响示例**：
- gentle: "本章节请用轻柔舒缓的节奏叙述，如微风拂面，叙事悠缓从容，让情感在字里行间缓缓流淌。"
- urgent: "本章节请用紧凑急促的节奏叙述，叙事明快紧张，情节推进迅速，不拖泥带水。"
- forceful: "本章节请用浓烈有力的节奏叙述，戏剧张力强，情感浓烈鲜明，叙事有分量感。"

**测试状态**: 前端 261/261 ✅，TypeScript 编译无错误

